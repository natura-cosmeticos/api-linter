import { mustContainServerURL } from './must-contain-server-url';
import { mustContainPort } from './must-contain-port';
import { noSingularResource } from './no-singular-resource';

export {
  mustContainServerURL,
  mustContainPort,
  noSingularResource
};
